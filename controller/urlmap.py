from controller.example.url import add_route as example
from controller.host.url import add_route as host
from controller.access.url import add_route as access

Routes_Map = {
    "example": example,
    "host": host,
    "access": access
}
